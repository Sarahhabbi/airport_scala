/*
import Model.{countries, loadCountries}

object Main {
  def main(args: Array[String]): Unit = {
    loadCountries()
    println(countries)
    countries.foreach
    {
      case (key, value) => println (key + " -> " + countries.get("France".trim))
    }
  }
}
*/
